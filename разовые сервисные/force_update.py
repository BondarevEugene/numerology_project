from app import app
from models import db

with app.app_context():
    print("⚠️ Подключение к базе для обновления структуры...")
    # Это создаст только ОТСУТСТВУЮЩИЕ колонки, если база поддерживает миграции,
    # но в нашем случае проще пересоздать, если данных еще нет:
    db.create_all()
    print("✅ Структура обновлена. Поля phone, gender, reset_token должны быть в базе.")